# セル結合デバッグ手順書

## 問題
編集画面でセルを結合させ保存したあともう一度確認すると結合がなくなっている。正しく保存されていないか、描画がおかしいかの原因を特定する。

## デバッグ環境の確認

### 1. デバッグコンソールの起動
- ブラウザでテンプレートエディタページを開く
- 画面右上の「🐛 Debug」ボタンをクリック
- または、ブラウザの開発者ツールのコンソールで `debugConsole.toggle()` を実行

### 2. デバッグ機能の確認
コンソールで以下を実行：
```javascript
checkDebugConsole()
```

## 問題再現とデバッグ

### 手動での問題再現
1. テンプレートエディタでセルを複数選択（例：2×2の範囲）
2. 「セル結合」ボタンをクリック
3. デバッグコンソールで結合ログを確認
4. テンプレート名を入力して「保存」をクリック
5. 保存時のデバッグログを確認
6. ページを再読み込みして同じテンプレートを開く
7. 読み込み時のデバッグログを確認
8. 結合セルが復元されているかを確認

### 自動テスト

#### ローカルテスト（データ変換テスト）
```javascript
debugMergeTest()
```
- 結合データの保存・読み込み変換をテスト
- Map → Array → Map の変換が正しく動作するか確認

#### APIテスト（実際の保存・読み込みテスト）
```javascript
debugApiMergeTest()
```
- 実際のAPI経由での保存・読み込みをテスト
- サーバーとクライアント間のデータ受け渡しを確認

## 確認すべきデバッグ出力

### 保存時に確認すべきログ
- `=== 保存時の結合データデバッグ ===`
- `SAVE_DEBUG: 結合セル数`
- `SAVE_DEBUG: 結合データ`
- `SAVE_DEBUG: JSON化された結合データ`

### 読み込み時に確認すべきログ
- `=== 読み込み時の結合データデバッグ ===`
- `LOAD_DEBUG: template.merged_cells`
- `LOAD_DEBUG: パースされた結合データ`
- `LOAD_DEBUG: 復元された結合データ`

### 復元時に確認すべきログ
- `=== 結合セル復元処理開始 ===`
- `RESTORE_DEBUG: 復元対象の結合データ`
- `RESTORE_DEBUG: 結合復元中`

## サーバーサイドのデバッグ

`/api/templates/create-web` エンドポイントでのサーバーサイドログも確認：
- `SERVER_DEBUG: 受信した結合データ`
- `SERVER_DEBUG: 結合データ処理中`
- `SERVER_DEBUG: 結合データ変換完了`

## 予想される問題箇所

1. **保存時**: JavaScript Map → Array 変換
2. **サーバー処理**: Array → JSON保存
3. **読み込み時**: JSON → Array → Map復元
4. **DOM復元**: Map → HTML DOM操作

## 利用可能なデバッグコマンド
- `debugHelp()` - デバッグ機能のヘルプ表示
- `checkDebugConsole()` - デバッグ環境確認
- `debugMergeTest()` - ローカルデータ変換テスト
- `debugApiMergeTest()` - API経由テスト

## トラブルシューティング

### デバッグコンソールが表示されない
- ページを再読み込み
- `debugConsole.toggle()` を実行

### ログが表示されない
- ブラウザの開発者ツールのコンソールタブを確認
- `console.debug` が有効になっているか確認

### テストが動作しない
- `editor` オブジェクトが初期化されているか確認
- エラーメッセージをコンソールで確認